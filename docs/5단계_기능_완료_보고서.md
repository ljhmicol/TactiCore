# 5단계: 기능 완료 보고서 (TactiCore)

> **현재 상태: 전 항목 미착수 (진행률 0%).** 이 문서는 구현 진행 상황을 추적하는 체크리스트다. 작업을 완료할 때마다 해당 항목의 상태 기호를 갱신하고, §8의 요약표를 함께 고친다. 실제로 동작을 확인하기 전에는 `[x]`로 바꾸지 않는다.
>
> 항목 번호는 `4단계_구현_상세_가이드.md` §1의 Phase Plan과 1:1로 대응한다.

## 범례

| 기호 | 의미 |
| --- | --- |
| `[ ]` | 미착수 |
| `[~]` | 진행 중 |
| `[x]` | 완료 (동작 확인됨) |
| `[!]` | 막힘 / 이슈 있음 |
| `[-]` | 보류 (범위에서 제외하기로 결정) |

---

## Phase 1. 백엔드 기반 (FR-08)

- [ ] 1-1. `backend/` 디렉토리 생성, venv 구성, `requirements.txt` 설치
  - [ ] 1-1-1. `config.py` — pydantic-settings로 `.env` 로드 (`DATABASE_URL`, `CORS_ORIGINS`)
- [ ] 1-2. `database.py` — 엔진/세션 구성
  - [ ] 1-2-1. `PRAGMA foreign_keys=ON` 이벤트 리스너 등록
  - [ ] 1-2-2. `check_same_thread=False` 설정
- [ ] 1-3. `models.py` — ORM 4개 테이블
  - [ ] 1-3-1. `Analysis` (스칼라 필드 + relationship)
  - [ ] 1-3-2. `Player` (`client_id` UNIQUE 제약 포함)
  - [ ] 1-3-3. `Phase` (`analysis_id`+`phase_type` UNIQUE)
  - [ ] 1-3-4. `Position` (`side`, `slot`, nullable `player_id`)
  - [ ] 1-3-5. 모든 relationship에 `cascade="all, delete-orphan"`
- [ ] 1-4. `schemas.py` — Pydantic 스키마
  - [ ] 1-4-1. 좌표 0~100 범위 검증
  - [ ] 1-4-2. `players` 11명 고정, id 중복 불가
  - [ ] 1-4-3. `phases` 3종 필수, 각 11좌표
  - [ ] 1-4-4. `opponentPositions` 생략 가능 / 존재 시 11개
  - [ ] 1-4-5. `matchDate` 형식, `analyzedTeam` enum
- [ ] 1-5. `crud.py`
  - [ ] 1-5-1. `upsert_analysis` (신규 저장)
  - [ ] 1-5-2. `upsert_analysis` (전체 교체 갱신)
  - [ ] 1-5-3. `to_analysis_json` — 3단계 §2.3 형태로 조립
  - [ ] 1-5-4. `client_id` 보존 확인
  - [ ] 1-5-5. 목록 조회 (좌표 미포함, 최근 수정순)
- [ ] 1-6. `routers/analyses.py` — 6개 엔드포인트
  - [ ] 1-6-1. `GET /api/health`
  - [ ] 1-6-2. `GET /api/analyses`
  - [ ] 1-6-3. `GET /api/analyses/{id}` (404 처리)
  - [ ] 1-6-4. `POST /api/analyses`
  - [ ] 1-6-5. `PUT /api/analyses/{id}`
  - [ ] 1-6-6. `DELETE /api/analyses/{id}` (204)
- [ ] 1-7. `main.py` — 앱 초기화, CORS(5173만 허용), 테이블 생성
- [ ] 1-8. Swagger(`/docs`)에서 6개 엔드포인트 수동 검증
  - [ ] 1-8-1. 저장 → 조회 → 수정 → 삭제 왕복 확인
  - [ ] 1-8-2. 삭제 시 하위 행 CASCADE 삭제 확인
  - [ ] 1-8-3. 검증 위반 요청이 422를 반환하는지 확인

## Phase 2. 프론트 기반 + 피치 (FR-01)

- [ ] 2-1. Vite + React + TS 프로젝트 생성
  - [ ] 2-1-1. Tailwind 설치 및 설정
  - [ ] 2-1-2. `tsconfig` path alias(`@/*`) 추가
  - [ ] 2-1-3. shadcn/ui init + 기본 컴포넌트 추가
- [ ] 2-2. `types/analysis.ts`, `lib/zones.ts` 상수 정의
- [ ] 2-3. `Pitch` — SVG `viewBox="0 0 100 100"`, 피치 라인 렌더링
- [ ] 2-4. `PlayerNode` — 원 + 등번호 + 이름 표시
- [ ] 2-5. 드래그 이동
  - [ ] 2-5-1. `clientToPitch` 좌표 변환 (`getScreenCTM().inverse()`)
  - [ ] 2-5-2. 0~99.9 클램프 (오버로드 경계 처리 대응)
  - [ ] 2-5-3. 드래그 중 transition 비활성화
- [ ] 2-6. `store/analysisStore.ts` — Zustand 편집 상태
  - [ ] 2-6-1. `movePlayer`가 현재 국면만 수정하는지 확인
  - [ ] 2-6-2. 레이어 토글을 분석 데이터와 분리
- [ ] 2-7. `lib/formations.ts` — 프리셋 4종 (FR-06)
- [ ] 2-8. `FormationPicker` + `/new` 라우트
- [ ] 2-9. `PlayerForm` — 선수 이름·등번호·역할 입력/수정 (FR-06)
  - [ ] 2-9-1. 등번호 1~99 범위 검증
  - [ ] 2-9-2. 경기 기본 정보(매치명·홈/원정팀·일자·대회명·분석 대상 팀) 입력 폼

## Phase 3. 국면 전환 + 텍스트 (FR-02, FR-04)

- [ ] 3-1. `PhaseTabs` — 기본/공격/수비 전환 UI
- [ ] 3-2. Framer Motion 좌표 보간
  - [ ] 3-2-1. **React key = `player.id`** (인덱스 사용 금지)
  - [ ] 3-2-2. `duration 0.6s`, `ease [0.4,0,0.2,1]`
  - [ ] 3-2-3. 전환 중 재전환 시 이어서 보간되는지 확인
- [ ] 3-3. `GhostLayer` — 이전 국면 잔상(opacity 0.25) + 이동 경로 점선
- [ ] 3-4. `isMorphing` 플래그 스토어 연동
- [ ] 3-5. `CommentPanel` — 국면별 코멘트 + 종합 평가
  - [ ] 3-5-1. 국면 전환 시 해당 국면 코멘트로 전환되는지 확인

## Phase 4. 공간 레이어 + 내보내기 (FR-03, FR-05, FR-07)

- [ ] 4-1. `ChannelGrid` — 5채널 경계선, 하프스페이스 강조
- [ ] 4-2. `lib/compactness.ts`
  - [ ] 4-2-1. 압박 라인 자동 산출 (GK = y 최대값 1명 제외)
  - [ ] 4-2-2. `pressingLineY` 수동 지정 시 자동 산출 무시
  - [ ] 4-2-3. bounding box + m 환산 표기 (y축 105m / x축 68m — 축 혼동 주의)
  - [ ] 4-2-4. `outfieldPlayers` 헬퍼를 압박 라인·콤팩트니스가 공유
- [ ] 4-3. `lib/overload.ts` — 15구역 계산
  - [ ] 4-3-1. 하한 포함 / 상한 배제, **단 마지막 구역은 100 포함**
  - [ ] 4-3-2. `diff>=2 strong`, `diff==1 weak`
  - [ ] 4-3-3. 단위 테스트 작성 (Vitest)
  - [ ] 4-3-4. 좌표가 정확히 `100`인 선수가 구역에서 누락되지 않는지 테스트 (JSON 가져오기 경로)
- [ ] 4-4. `OverloadLayer` — 구역 채색
  - [ ] 4-4-1. 상대 좌표 없을 때 토글 disabled + 안내 툴팁
- [ ] 4-5. `ShareCard` — 고정 1080px 카드 노드
  - [ ] 4-5-1. 1:1 / 4:5 비율 선택
  - [ ] 4-5-2. 화면 밖 배치 (`left:-9999px`)
  - [ ] 4-5-3. 하단 텍스트를 국면 코멘트 / 종합 평가 중 선택 (FR-04의 `summary` 출력 경로)
- [ ] 4-6. `lib/exportImage.ts`
  - [ ] 4-6-1. `isMorphing` 대기
  - [ ] 4-6-2. `document.fonts.ready` 대기
  - [ ] 4-6-3. `pixelRatio: 2` 캡처 + 다운로드
- [ ] 4-7. JSON 입출력 (FR-07)
  - [ ] 4-7-1. `lib/schema.ts` zod 검증 (서버와 동일 규칙)
  - [ ] 4-7-2. 내보내기 — 직렬화 함수를 저장 로직과 공유
  - [ ] 4-7-3. 가져오기 + 오류 위치 안내 문구
  - [ ] 4-7-4. 샘플 JSON 로드 경로 확인

## Phase 5. 저장·목록 연동 (FR-08)

- [ ] 5-1. `lib/api.ts`
  - [ ] 5-1-1. snake_case ↔ camelCase 변환
  - [ ] 5-1-2. `phases` 맵 키가 변환에서 보호되는지 확인
  - [ ] 5-1-3. 422 응답의 `loc`을 사람이 읽을 문구로 변환
- [ ] 5-2. `useServerHealth` — 서버 미기동 감지
  - [ ] 5-2-1. 서버 없이도 편집·PNG·JSON이 동작하는지 확인 (FR-08 폴백)
  - [ ] 5-2-2. 저장/목록 UI만 비활성화 + 안내 표시
- [ ] 5-3. React Query 연동
  - [ ] 5-3-1. `useAnalyses` 목록 조회
  - [ ] 5-3-2. 저장(POST/PUT) 뮤테이션 + 캐시 무효화
  - [ ] 5-3-3. 삭제 뮤테이션
- [ ] 5-4. `AnalysisList` 화면 + `/analyses/:id` 불러오기
  - [ ] 5-4-1. 불러온 뒤 `player.id`가 보존되어 모핑이 정상인지 확인
- [ ] 5-5. 삭제 확인 다이얼로그

## Phase 6. 마감

- [ ] 6-1. 샘플 경기 JSON 1건 작성 (`public/samples/`)
- [ ] 6-2. `scripts/dev.ps1` 동시 기동 확인
- [ ] 6-3. 실전 검증
  - [ ] 6-3-1. 실제 경기 1개 분석 (조작감 점검)
  - [ ] 6-3-2. 실제 경기 2개째 분석 (반복 사용성 점검)
  - [ ] 6-3-3. 생성된 PNG 카드 품질 점검 (텍스트 가독성, 해상도)
  - [ ] 6-3-4. 모바일 뷰 확인 (iOS Safari / Android Chrome)
- [ ] 6-4. 문서 마감
  - [ ] 6-4-1. README 작성
  - [ ] 6-4-2. `6단계_운영_매뉴얼.md`의 명령어 실검증 및 정정
  - [ ] 6-4-3. 2차 업데이트 범위 재정리 (피드백 반영)

---

## 7. 비기능 요구사항 검증

`1단계_요구사항_정의.md` §4 대응. 구현 완료 후 점검한다.

- [ ] 7-1. 라이트 모드 / 한국어 단일 언어 확인
- [ ] 7-2. 데스크톱 레이아웃 (좌 피치 / 우 패널)
- [ ] 7-3. 모바일 레이아웃 (상 피치 / 하 패널)
- [ ] 7-4. 데스크톱 Chrome·Safari 동작 확인
- [ ] 7-5. 모바일 iOS Safari·Android Chrome 동작 확인
- [ ] 7-6. 국면 전환 애니메이션 체감 성능 (60fps 목표 — **가정값이므로 미달 시 사용자와 조정**)
- [ ] 7-7. PNG 캡처 3초 이내 (가정값)
- [ ] 7-8. 로컬 API 응답 500ms 이내 (가정값)
- [ ] 7-9. `tacticore.db`가 `.gitignore`에 포함되어 커밋되지 않는지 확인

## 8. 완료 요약

| Phase | 범위 | FR | 상태 | 진행률 |
| --- | --- | --- | --- | --- |
| Phase 1 | 백엔드 기반 | FR-08 | 미착수 | 0% |
| Phase 2 | 프론트 기반 + 피치 | FR-01, FR-06 | 미착수 | 0% |
| Phase 3 | 국면 전환 + 텍스트 | FR-02, FR-04 | 미착수 | 0% |
| Phase 4 | 공간 레이어 + 내보내기 | FR-03, FR-05, FR-07 | 미착수 | 0% |
| Phase 5 | 저장·목록 연동 | FR-08 | 미착수 | 0% |
| Phase 6 | 마감 | — | 미착수 | 0% |
| **전체** | | | **미착수** | **0%** |

## 9. 이슈 기록

발생한 문제와 해결 여부를 여기에 누적한다. 형식: `날짜 / 항목번호 / 증상 / 조치`.

| 날짜 | 항목 | 증상 | 조치 | 상태 |
| --- | --- | --- | --- | --- |
| — | — | (아직 없음) | — | — |
